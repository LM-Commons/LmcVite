const packageConfig = {
  packageName: 'lmc-vite',  // e.g. lmc-authentication
  title: 'LmcVite', // e.g. LMC Authentication
  tagline: 'Laminas View Helpers for Vite', // e.g. Authentication for Mezzio applications
  projectName: 'lmc-vite', // the repo name (e.g. lmc-authentication).
};

export default packageConfig;